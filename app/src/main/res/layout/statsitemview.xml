<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="300dp"
    android:layout_height="wrap_content">

    <ImageView
        android:id="@+id/imageStatsItem"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:scaleX="0.3"
        android:scaleY="0.3"
        android:translationY="2dp"
        app:srcCompat="@drawable/retryicon" />

    <TextView
        android:id="@+id/textViewStatItem"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:layout_toEndOf="@+id/imageStatsItem"
        android:gravity="center"
        android:text="Preguntas falladas:"
        android:textColor="@color/black" />

    <TextView
        android:id="@+id/intItemStat"
        android:layout_width="40dp"
        android:layout_height="60dp"
        android:layout_toEndOf="@+id/textViewStatItem"
        android:gravity="center"
        android:text="500" />
</RelativeLayout>